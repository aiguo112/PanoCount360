# Optional: install GUI-only deps if you already have the base stack.
# Full install: pip install -r requirements.txt

PySide6>=6.5.0
matplotlib>=3.7.0
opencv-python>=4.8.0
numpy>=1.24.0
torch>=2.0.0
torchvision>=0.15.0
